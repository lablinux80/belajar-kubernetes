apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    Team: finance
    version: 3.3.1
    environment: production
spec:
  containers:
    - name: nginx
      image: nginx
      ports:
        - containerPort: 80
